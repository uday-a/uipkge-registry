import { describeBlock } from '../../../test-utils/test-render'
import LeafletDisasterResponseMap from '../LeafletDisasterResponseMap.vue'

describeBlock('LeafletDisasterResponseMap', LeafletDisasterResponseMap)
