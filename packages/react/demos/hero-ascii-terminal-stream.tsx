import { HeroAsciiTerminalStream } from '@/components/blocks/hero-ascii-terminal-stream'
import { Story } from '@/components/story/Story'

export default function HeroAsciiTerminalStreamDemo() {
  return (
    <Story
      title="Default"
      description="Retro-futuristic ASCII terminal stream hero section with interactive command executor."
    >
      <HeroAsciiTerminalStream />
    </Story>
  )
}
