import { HeroGlobeInteractiveNodes } from '@/components/blocks/hero-globe-interactive-nodes'
import { Story } from '@/components/story/Story'

export default function HeroGlobeInteractiveNodesDemo() {
  return (
    <Story title="Default" description="Global edge CDN node simulation hero with live telemetry inspector.">
      <HeroGlobeInteractiveNodes />
    </Story>
  )
}
