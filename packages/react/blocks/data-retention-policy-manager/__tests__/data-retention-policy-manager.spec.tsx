import { describeBlock } from '../../../test-utils/test-render'
import { DataRetentionPolicyManager } from '../DataRetentionPolicyManager'

describeBlock('DataRetentionPolicyManager', DataRetentionPolicyManager)
